---
title: "TiffExpectedFormat 枚举"
type: docs
weight: 60
url: /zh/python-net/aspose.psd.fileformats.tiff.enums/tiffexpectedformat/
---

期望的 TIFF 文件格式。

**Module:** [aspose.psd.fileformats.tiff.enums](/psd/python-net/aspose.psd.fileformats.tiff.enums/)

**Full Name:** aspose.psd.fileformats.tiff.enums.TiffExpectedFormat

**Aspose.PSD Version:** 24.12.0

## **Members**
| **成员名称** | **Description** |
| :- | :- |
| 默认 | 默认的 tiff 格式是不压缩，仅限 B/W 每像素 1 位的格式。您也可以使用此设置获取空选项，并使用您的标签或其他设置进行初始化。 |
| TIFF_CCITT_FAX3 | tiff CCITT FAX3 编码。仅在该方案中支持 B/W 每像素 1 位。 |
| TIFF_CCITT_FAX4 | tiff CCITT FAX4 编码。仅在该方案中支持 B/W 每像素 1 位。 |
| TIFF_CCIT_RLE | tiff CCITT RLE 编码。仅在该方案中支持 B/W 每像素 1 位。 |
| TIFF_DEFLATE_BW | tiff 使用 deflate 压缩，且仅限 B/W 每像素 1 位的格式。 |
| TIFF_DEFLATE_RGB | tiff 使用 deflate 压缩，且为 RGB 彩色格式。 |
| TIFF_DEFLATE_RGBA | tiff 使用 deflate 压缩，且为 RGBA 彩色格式。 |
| TIFF_JPEG_RGB | tiff 使用 Jpeg 压缩，且为 RGB 彩色格式。 |
| TIFF_JPEG_Y_CB_CR | tiff 使用 Jpeg 压缩，且为 YCBCR 彩色格式。 |
| TIFF_LZW_BW | 此 tiff 使用 LZW 压缩，仅限 B/W 每像素 1 位的格式。 |
| TIFF_LZW_CMYK | 此 tiff LZW cmyk |
| TIFF_LZW_RGB | 此 tiff 使用 LZW 压缩，RGB 颜色格式。 |
| TIFF_LZW_RGBA | 此 tiff 使用 LZW 压缩，RGBA 带透明度的颜色格式。 |
| TIFF_NO_COMPRESSION_BW | 未压缩的 tiff，仅限 B/W 每像素 1 位的格式。 |
| TIFF_NO_COMPRESSION_RGB | 未压缩的 tiff，RGB 颜色格式。 |
| TIFF_NO_COMPRESSION_RGBA | 未压缩的 tiff，RGBA 带透明度的颜色格式。 |
