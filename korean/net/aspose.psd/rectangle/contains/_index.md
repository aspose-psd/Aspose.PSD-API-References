---
title: "Rectangle.Contains"
second_title: "Aspose.PSD for .NET API 레퍼런스"
description: "Rectangle 메서드. 지정된 점이 이 Rectangle 구조 내에 포함되는지 확인합니다."
type: docs
weight: 220
url: /ko/net/aspose.psd/rectangle/contains/
---
{{< psd/tize >}}
## Contains(int, int) {#contains_2}

지정된 점이 이 [`Rectangle`](../) 구조 내에 포함되는지 확인합니다.

```csharp
public bool Contains(int x, int y)
```

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| x | Int32 | 테스트할 점의 x좌표. |
| y | Int32 | 테스트할 점의 y좌표. |

### 반환 값

이 메서드는 *x*와 *y*로 정의된 점이 이 [`Rectangle`](../) 구조 내에 포함되면 true를 반환하고, 그렇지 않으면 false를 반환합니다.

### 또 보기

* struct [Rectangle](../)
* namespace [Aspose.PSD](../../../aspose.psd/)
* assembly [Aspose.PSD](../../../)

---

## Contains(Point) {#contains}

지정된 점이 이 [`Rectangle`](../) 구조 내에 포함되는지 확인합니다.

```csharp
public bool Contains(Point point)
```

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| point | Point | 테스트할 [`Point`](../../point/). |

### 반환 값

이 메서드는 *point*로 표현된 점이 이 [`Rectangle`](../) 구조 내에 포함되면 true를 반환하고, 그렇지 않으면 false를 반환합니다.

### 또 보기

* struct [Point](../../point/)
* struct [Rectangle](../)
* namespace [Aspose.PSD](../../../aspose.psd/)
* assembly [Aspose.PSD](../../../)

---

## Contains(Rectangle) {#contains_1}

*rect*로 표현된 사각형 영역이 이 [`Rectangle`](../) 구조에 완전히 포함되는지 확인합니다.

```csharp
public bool Contains(Rectangle rect)
```

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| rect | Rectangle | 테스트할 [`Rectangle`](../). |

### 반환 값

이 메서드는 *rect*로 표현된 사각형 영역이 이 [`Rectangle`](../) 구조에 완전히 포함되면 true를 반환하고, 그렇지 않으면 false를 반환합니다.

### 또 보기

* struct [Rectangle](../)
* namespace [Aspose.PSD](../../../aspose.psd/)
* assembly [Aspose.PSD](../../../)


