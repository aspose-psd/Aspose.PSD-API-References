---
title: Layer.IsVisibleInGroup
second_title: .NET API 참조용 Aspose.PSD
description: Layer 재산. 이 인스턴스가 그룹에 있는지 여부를 나타내는 값을 가져옵니다레이어가 그룹에 없으면 루트 그룹을 의미합니다.
type: docs
weight: 180
url: /ko/net/aspose.psd.fileformats.psd.layers/layer/isvisibleingroup/
---
## Layer.IsVisibleInGroup property

이 인스턴스가 그룹에 있는지 여부를 나타내는 값을 가져옵니다(레이어가 그룹에 없으면 루트 그룹을 의미합니다).

```csharp
public virtual bool IsVisibleInGroup { get; }
```

### 자산 가치

`진실` 이 인스턴스가 그룹에서 보이는 경우; 그렇지 않으면,`거짓` .

### 또한보십시오

* class [Layer](../)
* 네임스페이스 [Aspose.PSD.FileFormats.Psd.Layers](../../layer/)
* 집회 [Aspose.PSD](../../../)


